---
import BaseLayout from "./BaseLayout.astro";
const { frontmatter } = Astro.props;

const date = new Date(frontmatter.pubDate);
const englishDate = date.toLocaleDateString("en-US", {
  year: "numeric",
  month: "long",
  day: "numeric",
});
---

<BaseLayout pageTitle={frontmatter.title}>
  <article
    class="prose prose-sm leading-7 text-neutral-950 max-w-none prose-a:no-underline prose-a:text-dark-red hover:prose-a:underline prose-code:text-sm"
  >
    <p class="text-neutral-400 text-sm">
      Published on: {englishDate}
    </p>
    <p><em>{frontmatter.description}</em></p>
    <slot />
  </article>
</BaseLayout>
